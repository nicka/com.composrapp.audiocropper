# iOS Crop audio 

WARNING: This is not working :)

## GET STARTED

1. npm install
2. Check `example/app.js`
3. `gulp ios` to build and test


### Needs to be fixed

`cropStartMarker` and `cropEndMarker` need to be floats.


Simulator crashes on:

```
[[NSFileManager defaultManager] removeItemAtURL:audioFileOutput error:NULL];
```
